# Not found

Template: `templates/404.liquid`

Rendered when the Liquid storefront receives an unmatched page request or a route fails to load a public resource.

## Direct route context

- no extra top-level route object beyond `store` and `request`

## Caveats

- This template is only used for page-like requests. Missing asset files still return a plain HTTP 404 response.
